services:
    backend:
        build:
            context: ./portfolio-backend
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        secrets:
            - mongo_uri
secrets:
    mongo_uri:
        file: ./secrets/mongo-uri.txt
